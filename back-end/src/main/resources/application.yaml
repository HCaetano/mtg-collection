spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/mtg-collection
    username: postgres
    password: senha
    driverClassName: org.postgresql.Driver